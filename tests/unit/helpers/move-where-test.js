import { moveWhere } from 'fridg2-ember/helpers/move-where';
import { module, test } from 'qunit';

module('Unit | Helper | move where');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moveWhere([42]);
  assert.ok(result);
});
