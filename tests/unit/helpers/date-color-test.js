import { dateColor } from 'fridg2-ember/helpers/date-color';
import { module, test } from 'qunit';

module('Unit | Helper | date color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateColor([42]);
  assert.ok(result);
});
