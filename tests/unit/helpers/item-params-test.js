import { itemParams } from 'fridg2-ember/helpers/item-params';
import { module, test } from 'qunit';

module('Unit | Helper | item params');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = itemParams([42]);
  assert.ok(result);
});
